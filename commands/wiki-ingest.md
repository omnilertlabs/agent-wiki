---
description: Ingest durable knowledge into the agent-wiki (append --dry-run to preview without writing)
---

Use the wiki-ingest skill to capture the following into the wiki. If its steps aren't in
your context after the skill loads, read the installed procedure at
`~/.claude/plugins/cache/*/agent-wiki/*/skills/wiki-ingest/SKILL.md` and follow it. If the
arguments include `--dry-run`, follow the skill's dry-run path (emit a change plan, write
nothing). $ARGUMENTS
