---
description: Lint the agent-wiki (structural Tier 1; add deep for semantic Tier 2)
---

Use the wiki-lint skill to lint this repo's `.claude/wiki/`. If its steps aren't in your
context after the skill loads, read the installed procedure at
`~/.claude/plugins/cache/*/agent-wiki/*/skills/wiki-lint/SKILL.md` and follow it. If the
user passed "deep", also run the Tier 2 agentic checks. Arguments: $ARGUMENTS
